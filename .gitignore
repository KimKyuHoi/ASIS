node_modules
dist
out
.DS_Store
.eslintcache
*.log*

# 머신별 환경 — .env.example 을 카피해서 본인 값 채우세요
.env

# pnpm 프로젝트 — npm이 생성하는 lockfile은 무시
package-lock.json

# TypeScript 증분 빌드 캐시 — 머신별로 다르므로 추적 불필요
*.tsbuildinfo
.claude/scheduled_tasks.lock
